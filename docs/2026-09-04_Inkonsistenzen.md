# Fotosite V08 — Geduldete Inkonsistenzen

*Stand: 04.09.2026*

Bekannte Abweichungen ohne Handlungsbedarf, sowie offene Klärungspunkte, die beim Dokumentations-Abgleich vom 19.07.2026 (Punkte 1–12) bzw. 31.07.2026 (Punkt 13) gefunden wurden. Punkt 7 am 04.08.2026 als behoben markiert. Punkte 14–15 am 29.08.2026 im Rahmen der subscriptiondb-Vorbereitung ergänzt (Punkt 14 direkt als behoben markiert). Punkt 16 am 04.09.2026 im Rahmen des Galeristen-Löschschutz-Umbaus ergänzt.

| # | Befund | Begründung |
|---|---|---|
| 1 | Phasen-Tabelle: unterschiedliche Granularität in `Projektstatus.md` vs. `Notfall_Start.md` (21.–23.06. zusammengefasst vs. getrennt) | Inhaltlich korrekt, nur unterschiedliche Auflösung — bewusst so gelassen |
| 2 | `Chat_Konventionen.md`: Workflow-Beschreibung (Claude.ai Chat + Claude Code getrennt) gilt nur für Claude.ai-Nutzung | Dokument bleibt unverändert — nicht relevant für direkte Claude-Code-Arbeit |
| 3 | `cust_user.cust_2fa_opt_in` — Funktion noch ungeklärt | Feld existiert in DB, Verwendung wird zu einem späteren Zeitpunkt definiert |
| 4 | `cust_pcode.mand_sort_date` — Funktion noch ungeklärt | Feld existiert in DB, Verwendung wird zu einem späteren Zeitpunkt definiert |
| 5 | `mand_user.valid_to` — Funktion noch ungeklärt | Option für zahlende mand bei Zahlungsausfall — erst bei Einführung eines Bezahlmodells relevant |
| 6 | **`TRUSTED_DEVICE_DAYS` doppelt in `.env` gesetzt** (Zeile 17: `=1`, Zeile 97: `=7`) | Laravels `phpdotenv` überschreibt bereits gesetzte Variablen nicht — die erste Definition gewinnt, effektiv gilt aktuell `1` Tag. Passt zufällig zum gewollten Testbetrieb, ist aber ein Duplikat und muss vor Umstellung auf 7 Tage bereinigt werden. **Handlungsbedarf, nicht nur geduldet** — siehe Projektstatus #13 / PROJECT_CONTEXT #24 |
| 7 | **Logout-Button-Dialog:** „Zurück"-Button im Trusted-Device-Löschdialog (`logout-button.blade.php`) schloss nur den Dialog, keine separate „ohne Löschen abmelden"-Aktion. Eine Umbenennung zu „Verlassen ohne Löschen" (Tab schließen statt Dialog schließen) wurde besprochen, der Nutzer hatte die Umsetzung zunächst abgebrochen | **BEHOBEN 04.08.2026** (Tag `logout_dialog_close_window_ok`, Commit `859516d`) — Button heißt jetzt „Fenster schließen" und ruft zusätzlich `window.close()` auf; schlägt das browserseitig fehl (nicht per Skript geöffnete Tabs), bleibt das bisherige Fallback-Verhalten (Dialog schließen) erhalten. Entspricht `PROJECT_CONTEXT.md` Abschnitt 12, Punkt #27 (andere Nummerierung, dieselbe Datei-Zeile) — Details Abschnitt 10i dort |
| 8 | **E-Mail-Footer im Sie-Form trotz Du-Form-Mailtext** in `two-factor-code.blade.php`, `trusted-device-added.blade.php` **und** `cust-invite.blade.php` (dritte Datei erst beim Abgleich am 19.07. gefunden — war in der ursprünglichen Meldung nicht enthalten) | Niedrige Priorität, bewusst nicht behoben. „Bitte antworten Sie nicht..." statt „Bitte antworte nicht..." |
| 9 | **Tag `cust_upload_popup_removed_ok` existiert nicht** in der Git-Historie, obwohl in einer Chat-Zusammenfassung als Beleg für die Entfernung des Upload-Bedingungen-Popups (cust) genannt | Die tatsächliche Änderung ist vorhanden und korrekt (Commit `c74f6a4`), nur unter anderem Tag: `cust_ds_hinweis_ok`. Reine Namens-Diskrepanz in der Doku, kein funktionaler Fehler |
| 10 | **Anon-Login-„Ausnahme" bei `regenerate()` existiert nicht** — eine frühere Chat-Zusammenfassung behauptete, der anon-Kurzzeit-Kennwort-Login nutze bewusst `regenerate()` ohne `destroy`, weil kein Login-Wechsel stattfinde | Tatsächlich wurde `CustLoginController::handleAnonLogin()` im Commit `8b4a875` ebenfalls auf `regenerate(true)` umgestellt — identisch zu allen anderen 6 Login-Stellen. Es gibt keine Ausnahme mehr. **Korrigiert in PROJECT_CONTEXT.md Abschnitt 10d/Header und Notfall_Start.md** |
| 11 | **Phase 6 (Passkey) fälschlich als „✓ Fertig"/„abgeschlossen" dargestellt — korrigiert 19.07.** Die Passkey-Funktionalität (WebAuthn, Registrierung/Login/Verwaltung für mand+cust) ist **technisch implementiert**, aber **noch nicht gründlich getestet**. Das betrifft nicht nur iOS: Getestet wurde bisher nur punktuell (Windows Hello, Android Chrome/Firefox, cust-Banner, ein Grenzfall mehrerer Rollen) — ein systematischer Test über alle Rollen × Geräte × Browser × Grenzfälle fehlt komplett, unabhängig von iOS. Zusätzlich ist speziell für iOS kein abgeschlossener WebAuthn-Passkey-Test dokumentiert; seit 09.07. wurde iOS zwar nachweislich getestet (Commit `b64d3ce`: „Getestet auf iOS/Windows/Android"), aber für Button-Feedback/Long-Tap-Fix und Auto-Login — nicht für den Passkey-Flow selbst | **Kein geduldeter Befund, sondern offener Handlungsbedarf.** Der gründliche Gesamttest der Passkey-Funktionalität ist der **explizite nächste Schritt für den neuen Chat**, vor Fortsetzung von Phase 7. Siehe PROJECT_CONTEXT.md Abschnitt 9/16a-0, Notfall_Start.md Abschnitt 5.2/7, Projektstatus.md Abschnitt 6 |
| 12 | **`passkey.sign_count` faktisch wirkungslos** — wird bei Registrierung und nach jedem Login beschrieben (`sign_count`/`last_used_at`-Update), aber beim Login nie wieder ausgelesen oder geprüft: der WebAuthn-Rollback-Schutz gegen geklonte Credentials greift nicht, da der `public_key`-Blob nach der Registrierung nie aktualisiert wird (`PasskeyRepository::saveCredentialSource()` existiert, wird im gesamten Code aber nie aufgerufen) — die Bibliothek prüft bei jedem Login intern gegen den eingefrorenen Registrierungs-Wert statt gegen den zuletzt gesehenen Stand. Zusätzlich liefern die hier ausschließlich genutzten Plattform-Authenticatoren (Windows Hello, Touch/Face ID, Android-Biometrie) den Zähler laut WebAuthn-Standard meist dauerhaft als `0` — als niederschwelliges Nutzungs-Tracking ungeeignet, dafür ist `last_used_at` die verlässliche Größe | Kein akutes Sicherheitsrisiko (nur entgangener Zusatzschutz gegen geklonte Credentials), bewusst nicht behoben. Diagnose vom 30.07.2026 — betrifft `CustPasskeyController.php`/`MandPasskeyController.php`:248, `PasskeyRepository.php`:46-103, `CustLoginController.php`:508-512, `MandantLoginController.php`:336-340 |
| 13 | **Geprüft: KEINE latente credentials-Error-Bag-Kollision zwischen mand und syst** — der Commit `error_messages_dirty_fix_ok` (29.07.) führte benannte Error-Bags (`'mand'`, `'syst'`, `'anon'`) ein, weil `auth/login-modal.blade.php` cust-, anon- UND mand-Login **auf derselben Seite** rendert (Tab-Kollisionsgefahr bei ungenannten Bags). syst läuft dagegen auf einer komplett separaten Seite (`system/login.blade.php`, nutzt ausschließlich `$errors->syst`, kein `login-modal.blade.php`-Include) und wird nie in derselben Response wie mand gerendert — eine Kollision ist strukturell ausgeschlossen, auch wenn beide zufällig denselben Bag-Namen verwendet hätten | Bewusst geprüft bei der Doku-Aktualisierung vom 31.07.2026, analog zum bereits dokumentierten und behobenen cust/mand-Fall. Kein Handlungsbedarf, keine Korrektur nötig |
| 14 | **`entry_type`-ENUM in `subscriptiondb.ledger_entry` wurde mit dem Wert `'LS'` statt `'ZA'` (Zahlungsausgang) ausgeführt** — Bug bei der DDL-Erstellung, keine bewusste Umbenennung gegenüber dem Konzept (`docs/Konzept_Abrechnungssystem.md` Abschnitt 3.4) | **BEHOBEN 29.08.2026** — `ALTER TABLE ledger_entry MODIFY entry_type ENUM('FO','GG','ZE','ZG','ZA') NOT NULL` + `UPDATE ledger_entry SET entry_type='ZA' WHERE entry_type='LS'` ausgeführt. Keine Code-Referenzen betroffen (Anwendungslogik existierte noch nicht, per `grep` geprüft). `docs/u14bc1w8_v08_subscriptiondb.sql` entsprechend korrigiert |
| 15 | **FK-Constraints in `subscriptiondb` abweichend vom Projektstandard** (sonst nur ein einziger echter FK im gesamten Schema, siehe PROJECT_CONTEXT.md Abschnitt 4) | Bewusste Entscheidung, dokumentiert in `Konzept_Abrechnungssystem.md` Abschnitt 5.4 — die FKs innerhalb `subscriptiondb` dokumentieren die Verwendung der Datenbank und machen das Modell ohne zusätzliche Erklärung lesbar. Kein Handlungsbedarf |
| 16 | **`ROUTES USED` in `system/mandanten/edit.blade.php` unvollständig** — `system.mandanten.toggle-active` und `system.mandanten.destroy` fehlen im Datei-Kopf-Docblock, obwohl seit dem Galeristen-Löschschutz-Umbau (04.09., Abschnitt 10o) in der Datei verwendet | **Offen — kosmetisch, keine funktionale Auswirkung** |

Fotosite V08 — Geduldete Inkonsistenzen | Stand 04.09.2026
